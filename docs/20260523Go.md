# Development Log — May 23, 2026

---

## Backend File Structure

```
main.go (Mother ng lahat)
  │
  ├── connects to → utils/
  │                   ├── db.go (PostgreSQL connection)
  │                   ├── redis.go (Redis connection)
  │                   ├── email.go (SMTP email sending — pure Go)
  │                   ├── otp.go (OTP generation — pure Go)
  │                   ├── totp.go (TOTP logic — pure Go)
  │                   └── .env (config: DB, SMTP, etc.)
  │
  ├── connects to → credential/
  │                   │
  │                   ├── credential.go (Mother — RegisterRoutes)
  │                   │     │
  │                   │     ├── login.go
  │                   │     ├── logout.go
  │                   │     ├── totp.go
  │                   │     ├── totpsession.go
  │                   │     ├── session.go
  │                   │     ├── session_expiry.go
  │                   │     ├── attempts.go (Redis-based)
  │                   │     └── credentiallogprint.go → tumatawag sa auditcredential
  │                   │
  │                   └── (lahat ng file dito, credential.go ang nag-coconnect)
  │
  ├── connects to → centralizeaudit/
  │                   │
  │                   ├── centralizeaudit.go (Mother — LogAction, HandleAuditLog)
  │                   │     │
  │                   │     └── auditcredential/
  │                   │           └── auditcredential.go (tumatawag sa centralizeaudit.go)
  │                   │
  │                   └── (kapag may bagong module, dagdag lang ng folder dito)
  │
  ├── connects to → apps/
  │                   └── apps.go (app listing, access control)
  │
  ├── connects to → appuserinfo/
  │                   └── rightbody/appuserinfoUSERDATA/userdata.go
  │
  └── connects to → userinfouserdata/
                      └── userinfouserdata.go (file upload)
```

---

## Frontend File Structure

```
src/app/ (Root)
  │
  ├── layout.tsx (Mother — wraps lahat ng pages)
  ├── page.tsx (Landing/redirect page)
  ├── globals.css (Global styles — Tailwind)
  │
  ├── login/
  │     │
  │     ├── page.tsx (Mother — login page)
  │     │     │
  │     │     └── components/
  │     │           ├── EmailStep.tsx
  │     │           ├── OtpStep.tsx
  │     │           └── AuthenticatorStep.tsx
  │     │
  │     └── (page.tsx ang nag-coconnect sa components)
  │
  ├── dashboard/
  │     │
  │     ├── page.tsx (Mother — dashboard page)
  │     │     │
  │     │     └── components/
  │     │           ├── LeftUpper.tsx
  │     │           ├── LeftLower.tsx
  │     │           ├── RightUpper.tsx
  │     │           └── RightLower.tsx
  │     │
  │     └── (page.tsx ang nag-coconnect sa components)
  │
  ├── 2fa/
  │     │
  │     ├── page.tsx (Mother — 2FA setup/manage page)
  │     └── components/
  │
  ├── appuserinfo/
  │     │
  │     └── rightbody/appuserinfoUSERDATA/
  │           └── page.tsx (User info module page)
  │
  └── centralizeaudit/
        │
        ├── centralizeaudit.ts (Mother — main fetch function)
        │     │
        │     ├── auditdashboard/
        │     │     └── auditdashboard.ts (tumatawag sa centralizeaudit.ts)
        │     │
        │     └── audituserinfo/
        │           └── audituserinfo.ts (tumatawag sa centralizeaudit.ts)
        │
        └── (kapag may bagong module, dagdag lang ng folder dito)
```

---

## Items Completed Today

### Item 1: Error Handling & Logging ✅
- Pinalitan lahat ng `fmt.Println` → `log.Println`
- May timestamp na sa lahat ng logs
- PM2 ang nag-mamanage ng backend process
- Gumawa ng `credentiallogprint.go` — hiwalay na file para sa lahat ng log functions

### Item 4: Audit Log ✅
- Gumawa ng `auditlog` table sa PostgreSQL

| Column | Purpose |
|--------|---------|
| id | BIGSERIAL Primary Key |
| emailx | Sino ang gumawa |
| action | Ano ang ginawa |
| module | Saang module |
| details | Specific info |
| ipaddress | IP address |
| reversedns | Hostname ng IP |
| useragent | Browser/device info |
| datemade | Timestamp |

- Backend: `centralizeaudit/centralizeaudit.go` — LogAction(), HandleAuditLog endpoint
- Backend: `centralizeaudit/auditcredential/auditcredential.go` — credential-specific audit
- Frontend: `centralizeaudit/centralizeaudit.ts` — main fetch function
- Frontend: `centralizeaudit/auditdashboard/auditdashboard.ts` — dashboard audit
- Frontend: `centralizeaudit/audituserinfo/audituserinfo.ts` — userinfo audit
- Endpoint: `/api/audit/log` — frontend calls this to log actions

### Actions na nare-record sa auditlog table:
| Action | Module | Trigger |
|--------|--------|---------|
| otp_sent | credential | Kapag nag-send ng OTP |
| login_otp | credential | Kapag na-verify ang OTP |
| login_totp | 2fa | Kapag na-verify ang TOTP |
| totp_setup | 2fa | Kapag nag-setup ng 2FA |
| totp_disabled | 2fa | Kapag nag-disable ng 2FA |
| logout | credential | Kapag nag-logout |
| open_module | dashboard | Kapag nag-click ng module |

### Item 7: Database Index ✅
- One-time SQL commands — walang code change

| Table | Column | Index Name |
|-------|--------|-----------|
| appaccess | emailx | idx_appaccess_emailx |
| userinfotbl | emailx | idx_userinfotbl_emailx |
| usertotp_sessions | emailx | idx_usertotp_sessions_emailx |
| usertotp_sessions | sessionid | idx_usertotp_sessions_sessionid |
| auditlog | emailx | idx_auditlog_emailx |
| auditlog | datemade | idx_auditlog_datemade |
| dcappaccess | emailx | idx_dcappaccess_emailx |

### Item 8: Redis for Attempt Tracking ✅
- Installed Redis sa server
- Installed `github.com/redis/go-redis/v9`
- Gumawa ng `utils/redis.go` — Redis connection
- Rewritten `credential/attempts.go` — in-memory map → Redis
- Attempt count hindi na mawawala kapag nag-restart ang server
- Auto-expiry: 60 seconds lockout, 5 minutes OTP expiry

---

## Items Skipped (For Later)

| # | Item | Reason |
|---|------|--------|
| 3 | Testing | Hindi urgent — gagawin kapag sobrang laki na ng project |
| 5 | Service Layer | Hindi applicable — current structure ay organized na per file per concern |
| 6 | API Documentation | Hindi urgent — kapag may bagong developer na sasali |

---

## Running the App

### Backend (PM2):
```bash
cd /home/eon/Desktop/CentralApp/backend
go build -o centralapp-backend .
pm2 restart centralapp-backend
```

### Frontend:
```bash
cd /home/eon/Desktop/CentralApp/frontend
npm run build && npm run start -- -p 3001
```

### Nginx:
- HTTPS sa port 443 (self-signed cert)
- HTTP port 80 → redirect sa HTTPS
- `/api/*` → Go backend (port 3000)
- `/*` → Next.js frontend (port 3001)

### Access:
- https://192.168.100.6

### Check logs:
```bash
pm2 logs centralapp-backend
```

### Check audit records:
```bash
export PGPASSWORD='password@01' && psql -h localhost -U users -d centraldb -c "SELECT * FROM auditlog;"
```
